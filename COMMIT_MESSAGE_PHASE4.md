Add Phase 4 scaffolding: Groups module, Prisma migration files, and schema additions.

This commit adds minimal, backward-compatible stubs for Phase 4 features so the repository can be extended safely.
